export const metadata = {
  title: "Lab dashboard — AWARE",
  description: "Manage your lab profile, updates, and team on AWARE.",
}

export default function LabDashboardLayout({ children }) {
  return children
}
